name: Env Variable & Secrets Outputs

on:
  workflow_dispatch:

jobs:
  env_variable:
    runs-on: ubuntu latest
    env:
      Role: Salesforce Devops Engineer
    steps:
      - name: Checkout code
        uses: Actions/checkout@v6

      - name: Print name using secrets
        run: echo "Name - ${{ secrets.MY_NAME }}"
          echo "Role - $Role"
